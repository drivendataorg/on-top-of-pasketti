"""``husky`` placeholder — empty module.

The upstream ``gezi.common`` does ``import husky`` (it was used by the
internal training framework for HTML logging). The standalone release
does not need it; this empty stub keeps the import statement valid.
"""
